Add the builded game version inside the folder "gameBuild", then cdouble click on scripts/startGame.sh. Enjoy.

If you don't want to build the game for debugging purposes, just run the game directly inside Godot, should be work.


